apiVersion: v1
kind: Service
metadata:
  name: game-store-service
  labels:
    app: game-store
spec:
  selector:
    app: game-store
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
